import os

Import('env')

src = [
    "baghash.c",
    "bitstream.c",
    "compress.c",
    "hash_state.c",
    "options.c",
]

libs = [
  "blake2b",
  "keccak",
]

env.Append(LIBS = libs)
env.StaticLibrary("baghash", src)

